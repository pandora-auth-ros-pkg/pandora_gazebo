<?xml version="1.0"?>

<robot xmlns:xacro="http://ros.org/wiki/xacro" >

  <xacro:macro name="gazebo_ros_control" params="interface" >
    <gazebo>
      <plugin name="GazeboRosControl" filename="libgazebo_ros_control.so" >
        <robotSimType>${interface}</robotSimType>
      </plugin>
    </gazebo>
  </xacro:macro>

</robot>
